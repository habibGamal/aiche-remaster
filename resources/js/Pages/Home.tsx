import React, { useState, useRef, useMemo } from 'react';
import JoditEditor from "jodit-react";
import { Button, Checkbox, Form, message, Input, Upload, UploadProps } from 'antd';
import { PlusOutlined } from '@ant-design/icons';
import TextArea from 'antd/lib/input/TextArea';

const Home = ({ test }: { test: string }) => {

    return (
        <h1 className="text-3xl text-center">HI , MR.Habib</h1>
    );
};
export default Home;
